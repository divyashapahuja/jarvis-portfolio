# `docs/`

Subfolder **`plans/`** holds committed `*.plan.md` blueprints (path: `docs/plans/`).

That is not the same as the **gitignored** **`/plans/`** directory at the **repository root** (see `.gitignore`).
